{
  "name": "release-party",
  "version": "0.1.0",
  "description": "GitHub Releases for versioning and management 🎉",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --outfile=dist/index.js && (node dist/index.js || true)",
    "package": "npm run build",
    "format": "prettier --write src/**/*.ts",
    "lint": "eslint src",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "octokit": "^5.0.5"
  },
  "devDependencies": {
    "@types/node": "^24.10.4",
    "@typescript-eslint/eslint-plugin": "^8.51.0",
    "@typescript-eslint/parser": "^8.51.0",
    "esbuild": "^0.27.2",
    "eslint": "^9.39.2",
    "prettier": "^3.7.4",
    "typescript": "^5.5.3",
    "vitest": "^4.0.16"
  },
  "private": true
}
